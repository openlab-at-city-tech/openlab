
  // Color Picker JS
  (function ( $ ) {

    // Add Color Picker to all inputs that have 'color-field' class
        $(
            function () {

                $('.my-color-field').wpColorPicker();
            }
        );
     
  })(jQuery);